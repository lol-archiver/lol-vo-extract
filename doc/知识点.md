# 知识点
- 一个`HIRCSound`里有一个Audio ID，Audio ID是wem文件名
- `HIRCAction`和`HICRSwtich`应该是引擎自动生成的ID，因为解包中发现这些类ID存在连续的情况。如果是某个文本的哈希是不可能连续的
- `HIRCSound`会被包含在`HIRCContainer`
- 一般`HIRCContainer`会直属`HIRCAction`。如果有形态方面的条件，会插入HIRCSwitch之类的层次
