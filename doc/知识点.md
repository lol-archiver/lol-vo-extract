# 知识点
- wpk中的文件wem文件名就是它的CRC32
- HIRCEventAction和HICRSwtich应该是引擎自动生成的ID，因为解包中发现这些类ID存在连续的情况。如果是文本的哈希是不可能连续的

mCharacterType
1 Enemy
2 TauntEnemy
3 TauntAlly

6 Kill CharacterMetadata
	mCategory
	mData
7 TauntEndedNearby TauntResponse


mRuleName: string = "Pentakill"
mConditions: list[pointer] = {
	ContextualConditionMultikillSize {
		mMultikillSize: u8 = 5
	}
}

mRuleName: string = "NearbyEnemyKilledChampionKillingSpree"
mConditions: list[pointer] = {
	ContextualConditionMultikillSize {
		mMultikillSize: u8 = 3
	}
}
